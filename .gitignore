# Dependencies & build outputs
node_modules/
dist/
out/
coverage/
*.tsbuildinfo
.eslintcache

# Claude Code session artifacts (.claude/skills/ IS tracked — project skills)
.claude/worktrees/
.claude/settings.local.json

# Logs & temp
*.log
*.tmp

# Local env files (nothing in this project should ever need one — zero network)
.env
.env.*

# OS cruft
.DS_Store
._*
Thumbs.db

# Editors
.idea/
.vscode/
*.swp

# Packaging artifacts
*.tgz

# graphify — track only what's needed for queries + --update (graph.json, manifest, AST cache)
graphify-out/.graphify_*
graphify-out/.needs_update
graphify-out/cache/
graphify-out/GRAPH_REPORT.md
graphify-out/cost.json
